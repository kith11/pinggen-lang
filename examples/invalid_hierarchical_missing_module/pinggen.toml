[package]
name = "invalid_hierarchical_missing_module"
version = "0.1.0"

[build]
name = "invalid_hierarchical_missing_module"
entry = "src/main.pg"
output = "build/invalid_hierarchical_missing_module"
