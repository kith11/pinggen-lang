[package]
name = "invalid_registry_unknown_package"
version = "0.1.0"

[registry]
index = "../registry_fixture/index.toml"

[build]
name = "invalid_registry_unknown_package"
entry = "src/main.pg"
output = "build/invalid_registry_unknown_package"

[[dependency]]
name = "missing"
version = "1.0.0"
