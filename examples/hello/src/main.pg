import std::{ io }

func keep_looping(value: int) -> bool {
    return value != 0;
}

func main() {
    let mut count = 3;
    while keep_looping(count) {
        io::println(count);
        count = count - 1;
    }
    io::println("pinggen online");
}
