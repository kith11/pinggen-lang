[package]
name = "registry_app"
version = "0.1.0"

[registry]
index = "../registry_fixture/index.toml"

[build]
name = "registry_app"
entry = "src/main.pg"
output = "build/registry_app"

[[dependency]]
name = "shared"
version = "1.0.0"
