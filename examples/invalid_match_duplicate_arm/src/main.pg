enum State {
    Idle,
    Ready,
}

func main() {
    let state = State::Ready;
    match state {
        State::Idle => {
            return;
        }
        State::Idle => {
            return;
        }
        State::Ready => {
            return;
        }
    }
}
