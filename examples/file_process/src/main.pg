import std::{ io, fs }

func main() {
    match fs::read_to_string("input.txt") {
        FsResult::Ok(text) => {
            io::println(text);
        }
        FsResult::Err(message) => {
            io::println(message);
        }
    }
}
