[package]
name = "invalid_dependency_both_sources"
version = "0.1.0"

[registry]
index = "../registry_fixture/index.toml"

[build]
name = "invalid_dependency_both_sources"
entry = "src/main.pg"
output = "build/invalid_dependency_both_sources"

[[dependency]]
name = "shared"
path = "../path_dependency_shared"
version = "1.0.0"
